#Область ОбработчикиСобытийЭлементовТаблицыФормыСписок

&НаКлиенте
Процедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь; // Отменяем стандартное закрытие, если нужно
    
    // Получаем данные текущей строки списка
    ДанныеСтроки = Элементы.Список.ТекущиеДанные;
    
    // Передаем структуру данных обратно в документ
    ОповеститьОВыборе(ДанныеСтроки);
    
КонецПроцедуры

#КонецОбласти